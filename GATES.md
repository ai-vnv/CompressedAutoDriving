# Gate Status

| Gate | Status | Evidence / blocker |
|---|---|---|
| F0 — problem-formulation scaffold | PASSED | State/observation/measurement/belief/action/privileged contracts konsisten. Optional road/stop-line fields adalah implementation conflict yang terdokumentasi untuk map tanpa geometry, bukan perubahan model matematika. |
| F1 — Gym-Duckietown integration | PASSED | Gym-Duckietown 6.2.0 nyata reset dan step melalui adapter. Agent menerima `SensorObservation`; privileged truth hanya melalui port terpisah; actual motion diturunkan dari pose; sign/unit tests nyata lulus. |
| F2 — action adapter + actuator envelope | PASSED | `PolicyAction -> ActionAdapter -> WheelCommand -> Simulator` teruji nyata. Sweep 14 kasus tersimpan di `artifacts/action_envelope.csv`; 0 clip, 0 off-road, 0 terminate. Positive/negative yaw dan actual feedback terukur. |
| F3 — minimal POMDP scenario | PASSED | Custom `pomdp_v1` map nyata load/step dengan lane, satu stop sign, stop line terpisah, dan tepat satu configurable Duckie pedestrian. Privileged fields non-None dan tidak bocor ke `SensorObservation`. |
| F4 — true-state validation | PASSED | Semua state terisi dalam unit locked. Lima controlled cases dan manual geometry checks lulus; signed stop distance menyeberang `+0.5200 -> -0.0577 m`. Evidence: `artifacts/state_validation.csv` (216 rows). |
| F5 — calibrated ground projection | PASSED | Re-audit F5b menemukan look-at extrinsic menambahkan pitch fiktif. Setelah disamakan dengan urutan OpenGL simulator, 108 real rendered samples menghasilkan raw origin-range MAE `0.01425 m`, RMSE `0.01698 m`, dan bearing RMSE `0.00942 rad`. Artifact pre-fix tetap disimpan untuk audit. |
| F5b — range semantics + calibration | PASSED | Origin dan nearest collision footprint dibandingkan eksplisit pada 120 real-rendered samples. Raw MAE: origin `0.01441 m`, surface `0.02680 m`; canonical range dipilih `object_origin`. Linear calibration fit pada 68 calibration-episode samples dan held-out pada 52 samples: bias `0.00036 m`, MAE `0.00515 m`, RMSE `0.00597 m`, SD `0.00601 m`. Runtime calibrator tidak menerima privileged truth; noise config diekspor. Full suite: 59 passed, 0 failed, 0 skipped. |
| A0 — preliminary action probe | SUPERSEDED | Probe awal di `artifacts/action_gate/report.json` digantikan oleh F2 CSV dengan transient/steady windows. |
| P0 — simulator camera | PASSED | Headless reset menghasilkan RGB `480x640x3 uint8`. F1 menguji frame ini pada agent path nyata. |
| F6 — oracle measurement model | PASSED | `oracle_clean/noisy/dropout` memakai real simulator truth pada boundary terpisah dan menghasilkan `ObjectMeasurement`. Monte Carlo 30,024 rows mereproduksi bias/SD tiga range bin, provisional bearing Gaussian, dan synthetic miss `0.201` vs config `0.200`; clean error nol. Seed reproducible dan missing memakai `None`. |
| F7 — pedestrian EKF belief updater | PASSED | Cartesian EKF memakai polar `r,beta`, F5b `R(r)`, actual ego-motion SE(2), explicit existence probability, dan Jacobian/covariance propagation teruji finite difference. Real 6-scenario x 3-mode run: noisy range/bearing RMSE `0.00236 m/0.00493 rad`; dropout `0.00274 m/0.00569 rad`. Position coverage dekat target tetapi rate intervals masih conservative dan tercatat, bukan disembunyikan. Full suite: 78 passed, 0 failed, 0 skipped. |
| F8-prep-A — simulator detection dataset | PASSED | 803 real Gym-Duckietown RGB frames dan 708 silhouette-derived boxes dibuat dengan mapping tunggal `0=stop_sign,1=duckie`; 376 valid negative frames tersedia. Privileged render difference hanya berada pada offline annotation path. Stop-sign orientation dikoreksi ke 180 derajat setelah visual QA menemukan sisi belakang. |
| F8-prep-B — dataset QA and split | PASSED | Split episode/seed/mode menghasilkan 471/168/164 train/val/test images; zero seed/episode leakage dan zero duplicate hashes across splits. Semua labels valid, distribution report dan 18 visual overlays tersimpan. |
| F8-prep-C — baseline YOLO training | PASSED | Satu YOLO11n baseline di-fine-tune 40 epochs pada train dengan validation-only model diagnostics: precision `0.96088`, recall `0.98774`, mAP50 `0.99243`, mAP50-95 `0.94575`. Checkpoint internal memiliki tepat dua project classes. |
| F8-prep-D — checkpoint readiness | PASSED | `artifacts/yolo_v1/best.pt` loads, SHA256 `3d4f816d...d7482c`, mapping internal tepat, dan image-only inference menemukan kedua target pada untouched-test sanity examples. Full suite: 89 passed, 0 failed, 0 skipped. |
| F8a — YOLO detector integration | PASSED | Frozen SHA256 cocok dan 164 held-out images dievaluasi pada fixed `conf=0.10`, NMS `0.70`, match IoU `0.50`. Stop sign: TP/FP/FN `62/8/0`, precision/recall `0.8857/1.0`, mAP50/50-95 `0.9939/0.9554`. Duckie: `84/3/0`, `0.9655/1.0`, `0.9949/0.9131`. Negative-frame false-alarm event: stop `6/102`, Duckie `0/80`. Far stop sign unsupported (`N=0`). Inference menerima RGB saja. |
| F8b — real measurement characterization | PASSED | 146 matched YOLO boxes diproyeksikan lewat satu F5 camera path dengan 0 failures. Raw range bias/MAE/RMSE/SD `0.10414/0.10680/0.17269/0.13822 m`; bearing `-0.00688/0.04222/0.05348/0.05322 rad`. F5b calibration menaikkan range RMSE ke `0.17494 m`, sehingga candidate F9 memakai raw projection dan diagonal `R=[0.0191051,0.00283261]`; global range residual non-Gaussian, bearing approximately Gaussian. F7 tetap frozen. Full suite: 94 passed, 0 failed, 0 skipped. |
| F9a — YOLO measurement calibration | PASSED | Calibration-only seeds `4101--4104` menghasilkan 1,193 matched Duckie measurements dengan 42/239/912 near/medium/far samples. Frozen additive bias `b_r=-0.0459048 m`, `b_beta=+0.00414568 rad`; diagonal range-bin sigma `0.003048/0.012491/0.016062 m`, bearing sigma `0.012648 rad`. `P_D=0.976678`; `P_FA=0.000780` dari 0/640 controlled hidden-Duckie RGB events. Artifact SHA256 `eb09ea6c...91b278` dibekukan sebelum seed final dibaca. |
| F9b — real YOLO to frozen EKF | LIMITED | Real RGB/frozen YOLO/frozen-F7 chain selesai pada 2,172 frame dari seed `5101--5104`; no privileged runtime leakage dan F7 config hash tetap. Bias-corrected range RMSE turun sedikit `0.02898 -> 0.02808 m`, tetapi bearing memburuk `0.00662 -> 0.00871 rad`; range bias masih `+0.01879 m`. Coverage range hanya `0.152/0.258` (68/95%), sehingga posterior sangat overconfident. Ada 57 natural misses, 78 duplicate frames, 16 IoU<0.5 localization updates, 0 class-negative FP, dan 0 false-track init. Hanya 8/57 miss frames mempertahankan active belief. Gate runtime/accuracy terlaksana, tetapi probabilistic calibration/control-readiness tidak passed; robust-observation review diperlukan. Full suite: 108 passed, 0 failed, 0 skipped. |
| F9c — robust observation and belief calibration | LIMITED (recommendation; human partner makes the final call) | Final evaluation complete on seeds `7101-7104` (3,328 frames, 40/40 episodes, rendered exactly once, reconstructed via hash-verified cache replay after a post-render metrics bug — never re-rendered). Frozen config SHA256 `359dc52020421c248bf6c26e036234191b2b97d24b505a66d85daa85b563704e`; runtime cache `fe425c55...722526d`; evaluation truth `26663ebb...d1e2ae9`. Robust B vs Baseline A: range RMSE `0.02024` vs `0.02580` (21.5% lower, meets the `<=1.15x` accuracy guard); bearing RMSE `0.01356` vs `0.01590` (14.8% lower); in-domain natural-miss belief retention `61.8%` (34/55) vs `18.2%` (10/55), clearing the `>=60%` criterion; gated-rejection retention `100%` (23/23), the direct payoff of separating detection evidence from kinematic acceptance (invariant I2); anti-inflation guard `std_over_rmse<=1.5` passes comfortably on both axes (`1.279`/`1.009`). **Not control-ready as pre-registered**: range coverage_68/95 (`0.852`/`0.988`) and bearing coverage_68 (`0.851`) all overshoot their pre-specified bands on the conservative side; the heavy-tail explanation for the overshoot was tested and refuted (z-distribution std `0.79`, excess kurtosis `-0.43`), leaving floor-transfer-to-a-disjoint-seed-set as an untested hypothesis. Outlier-impact evidence (n=9) is insufficient and points the wrong direction (Robust B `0.0346 m` vs Baseline A `0.0218 m`). Support minima all satisfied with wide margin (near `616`, medium `671`, far `1887`, edge_fov `543` vs minima `100/200/200/50`). 17 pre-specified PASS criteria tallied: 12 met, 3 not met (the three coverage-band misses), 2 insufficient evidence (the 20-consecutive-miss checkpoint and outlier-impact reduction). Leakage tests (`tests/test_f9c_leakage.py`, 6 tests, AST-based so genuine boundary-documenting comments/docstrings are never mistaken for leaks) confirm zero privileged/GT/evaluation-package references in the six runtime belief modules and zero mentions of the frozen F9b test seeds `5101-5104` anywhere under `src/`. Full detail, all 8 explicit questions, and the classification reasoning are in `IMPLEMENTATION_NOTES.md`'s "F9c gate report". Full suite: **251 passed, 0 failed, 0 skipped**. |
| F9d — control-readiness evidence closure | LIMITED | Frozen F9c was stress-tested without refit. A: once-only seeds `8201-8204`, 7,658 rows, 43 natural outlier frames / 29 events / 4 seeds. Events and seed spread passed, but frames missed the pre-registered 50 minimum, so A is `INSUFFICIENT_EVIDENCE`; descriptive Robust/Baseline range RMSE was favourable (`0.01648/0.06827 m`, ratio `0.241`). B: once-only seeds `8301-8304`, 7,260 rows, no warnings; B1/B2/B3 support all passed. B1 prediction-only recurrence matched to `4.44e-16`; B2 was below `P(e)=0.10` at frame 20 in 24/24 runs and recovered in one frame; B3 cleanly deleted 16/16 genuinely removed targets. Strict verifier: 15 PASS, 0 FAIL, 0 SKIP. Since CONTROL_READY requires both A and B, label remains LIMITED. Full active suite: **351 passed, 0 failed, 0 skipped**. Full report: `docs/superpowers/F9D_REPORT_FOR_REVIEW.md`. |
| F10-pre — SAC pre-training gate | PASSED | Leak-free 17-D observation, one normalized-to-physical action path, decomposed reward, canonical PyTorch SAC, development/final evaluator, and safety-first selection are implemented. Real evaluator smoke completed 180 steps on development seed `11001`; no final seed was used. Reward audit, 65-update SAC smoke, exact checkpoint reload, CUDA, W&B online target `vnv/DuckiePOMDP`, source/config provenance, and secret isolation all pass under config SHA256 `bef9e18d...a013`. Full suite: **397 passed, 0 failed, 0 skipped**. Evidence: `artifacts/f10/pretraining_gate.json`. |
| F10 — baseline SAC training and final evaluation | LIMITED | Leak-free RGB/YOLO/F9c-belief/SAC/action integration trained for 20,000 steps (19,001 updates; W&B run `kgmucdzw`). Development safety-first selection chose step 8,000 (`sac_baseline.pt`, SHA256 `79b98acb...c463`); step 20,000 remains `best_return`/`last` only. One-shot final evaluation on seeds `12001--12006` passed the collision check (`0/6`) but failed the other six competence checks: success `0%`, progress `0.290 m`, lane-departure episode rate `83.3%`, stop completion `33.3%`, and negligible pedestrian speed response. Full suite: **397 passed, 0 failed, 0 skipped**. Report: `docs/F10_REPORT_FOR_REVIEW.md`. |
| F10-L1 — `small_loop` CCW lane curriculum | PASSED | Separate lane-competence curriculum trained one canonical SAC run for 60,000 real-simulator steps (58,001 updates; W&B `z39mxtvl`). Safety-first development selection chose step 50,000 (`sac_lane_baseline.pt`, SHA256 `7d492fbf...2f72`). One-shot final evaluation on disjoint seeds `15001--15004`: 4/4 laps, zero invalid pose/yellow crossing/lane departure, mean `|d|=0.01062 m`, mean episode p95 `|d|=0.02502 m`, mean actual velocity `0.13976 m/s`. This is a six-state lane-control curriculum checkpoint, not the full YOLO/EKF deployment policy. Proof: `artifacts/f10_l1/sac_lane_demo.mp4`; report: `docs/F10_L1_REPORT_FOR_REVIEW.md`. Full suite: **411 passed, 0 failed, 0 skipped**. |
| F10-L2 — `experiment_loop` mixed-turn transfer | PASSED | Full-state SAC transfer from the hash-verified F10-L1 checkpoint trained once for 40,000 new-map steps (38,001 updates; W&B `y0qu681q`). Safety-first development selection rejected the collapsed 30k checkpoint and selected step 40,000 (`sac_lane_transfer_baseline.pt`, SHA256 `09a7fbcf...948a`). Once-only final evaluation on seeds `18001--18004`: 4/4 laps, zero invalid pose/yellow crossing/lane departure, mean `|d|=0.03413 m`, p95 `|d|=0.05763 m`, mean actual velocity `0.12980 m/s`. This remains a six-state lane curriculum checkpoint, not full YOLO/EKF deployment. Proof: `artifacts/f10_l2/sac_lane_transfer_demo.mp4`; report: `docs/F10_L2_REPORT_FOR_REVIEW.md`. Full suite: **419 passed, 0 failed, 0 skipped**. |
| F10-PPO-pre — staged belief-conditioned PPO | PASSED | Fixed 25-D belief/state input, canonical feed-forward PPO, one reward/action contract, deterministic seed-defined C0-C4 scenarios, retention/progression STOP guards, once-only global-final/report tooling, online W&B preflight, reward audit, and CUDA smoke are implemented. Fresh agent-follows-doc audit PASS; active suite: **446 passed, 0 failed, 0 skipped**. Full C0 remains fail-closed until the PASS witness is stored and the semantic gate validates every source/evidence hash. |
| F10-PPO-visual-v3 — camera-lane C0 curve-recovery retry | FAILED | The isolated v3 protocol made shallow curve contact recoverable while retaining deep/straight/recovery-failure terminals. Pretraining gate passed with **472 passed, 0 failed, 0 skipped**. Full C0 trained for 61,440 steps/60 PPO updates (W&B `oog0l05m`), but no training episode completed a lap and none of six checkpoints passed development. Diagnostic step 40,960 completed 1/4 held-out laps with 0% lane failure but 75% invalid pose and only 2.139 m mean progress. Stage-final seeds remain untouched and C1 was not started. Report: `docs/F10_PPO_VISUAL_RECOVERY_REPORT_FOR_REVIEW.md`. |
| F10-PPO-visual-v9 — camera-lane C0/C1 | PASSED | The fixed 29-D policy input uses front RGB -> frozen MobileNet lane estimator -> lane EKF, with neutral object beliefs in C0/C1. Counter-clockwise C0 and C1 stage-final gates passed on real Gym-Duckietown; report: `docs/F10_PPO_C0_C1_REPORT_FOR_REVIEW.md`. |
| F10-PPO-objects-v15 — C2 pedestrian | PASSED | The real runtime path adds frozen YOLO Duckie detection, metric projection, and F9c pedestrian belief before the 29-D PPO boundary. Once-only final: completion `2/4`, mean progress `7.8763 m`, collision/unsafe/invalid-pose `0/0/0`, minimum clearance `0.2325 m`; all frozen C2 skill, safety, and retention checks passed. Selected checkpoint SHA256 `e997f9bd...0042`. |
| F10-PPO-objects-v18 — C3 stop | PASSED | Separate stop-only map with Duckie physically absent; runtime stop sign is front RGB -> frozen YOLO -> metric projection -> stop belief -> fixed 29-D PPO. Development selected DAgger-distilled step 0 (`d058caee...f53c`); every 10k-40k on-policy PPO checkpoint failed. Once-only final seeds `166201-166204`: PPO `4/4` laps, `4/4` stop and restart, zero stop violation/collision/lane failure/invalid pose. Retention gate passed, but C1 retention is limited to 25% completion. W&B full run `g9x2c3c2`; active frozen suite **576 passed, 0 failed, 0 skipped**. Report: `docs/F10_PPO_C2_C3_REPORT_FOR_REVIEW.md`. C4 was not started. |
| F10-PPO-objects-v30 — C4 combined | PASSED | Runtime is front RGB -> frozen MobileNet lane belief + frozen YOLO/F9c pedestrian and stop beliefs -> fixed 29-D PPO. Offline privileged teacher labels were distilled into public-belief behavior targets; runtime actor/critic receive no privileged truth. Conservative 4,096-step PPO run W&B `2kp6inzo`; all four updated checkpoints passed development and safety-first selection chose eligible step 1,024 (`02e898ce...6250`). Once-only seeds `175201-175204`: `4/4` laps, stops, and restarts; zero collision, unsafe episode, stop violation, lane failure, invalid pose, or timeout; retention PASS across C0-C4. C1 retention remains limited to 25% completion. Post-stage suite **621 passed, 0 failed, 0 skipped**. Report: `docs/F10_PPO_REPORT_FOR_REVIEW.md`. No global-final was run; F11 is tracked separately below. |
| F11-IG — frozen C4 PPO Integrated Gradients | LIMITED | Four new explanation-only seeds (`176001-176004`) produced 9,554 public-belief frames; 2,389 stride-4 frames were explained for physical `v_cmd`, physical `omega_cmd`, and critic value. Checkpoint/model hashes remained unchanged and the saved trajectory contains no privileged truth. Completeness passed for all targets and adjacent-frame attribution ranks were stable. Velocity deletion faithfulness beat random (AUC ratio `1.309`), but yaw-rate did not (`0.953`), so steering IG is descriptive rather than a validated causal ranking. A spatial BEV extension replayed seed `176001` exactly (2,375 frames; zero observation/action drift) and maps 594 actor-IG samples while keeping world pose in a separate evaluation-only artifact. Method 2 semantic counterfactual ablation is recorded but unexecuted. Full suite after BEV extension: **633 passed, 0 failed, 0 skipped**. Report: `docs/F11_IG_REPORT_FOR_REVIEW.md`. |

Kandidat envelope saat ini adalah `v_max=0.4 m/s` dan
`omega_max=4.0 rad/s`. Kandidat ini belum menjadi batas permanen; penguncian
akhir menunggu trial lane-retention yang lebih panjang dan scenario geometry
yang representatif.
